server:
  port: 8090
feign:
  hystrix:
    enabled: true
hystrix:
  metrics:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  instance:
    prefer-ip-address: true